// Sentry isteğe bağlı — @sentry/nextjs kurulumuna gerek yok
// NEXT_PUBLIC_SENTRY_DSN set edildiğinde paketi manuel olarak ekle: npm i @sentry/nextjs
export function initSentry(): void {
  // Sentry entegrasyonu devre dışı — DSN yapılandırıldığında etkinleştir
}